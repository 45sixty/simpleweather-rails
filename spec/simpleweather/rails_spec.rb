require "spec_helper"

RSpec.describe Simpleweather::Rails do
  it "has a version number" do
    expect(Simpleweather::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
